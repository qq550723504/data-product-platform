# CSV 接入与工作区归属检查：集成验证

## 源码范围

基于 CSV 分支 `f2431685328e65b5315a9be11869f379df55b22f`，复用 #97
`2db88d3e5a5e41a229eb379860454da81da3fbe8` 的资源、数据集、实体解析归属检查及原有测试。
本候选不合并到 main，不改写 #91/#94/#97 的分支，不关闭这些 PR。

新增的集成处理：

- 数据集构造器同时兼容两参数和显式资源仓库参数；省略或 nil 都会建立真实的资源仓库依赖，**不跳过检查**。多个可选依赖直接报编程错误，不能静默选择。
- 数据集和实体解析 HTTP 错误使用稳定代码及安全说明，不把错误内部的其他工作区 UUID 回传。
- 新 HTTP/PostgreSQL 回归覆盖跨工作区来源、输入、输出，以及 RAW/CURATED/PRODUCT 错误输出类型。比较拒绝前后业务记录、Audit、Evidence 和工作区事件，并通过计数存储确保拒绝时未访问对象。
- 保留已有 CSV BOM 原字节校验、浏览器上传/审核、原生 Worker 四条依赖血缘及完整演示重启测试。

构造器兼容层防止 #97 的新参数使较新的 `cmd/poc-demo` 与真实浏览器测试在组合后编译失败；不能把分别绿色的 PR 视为已验证的组合。

## 验证层级

`TestWorkspaceOwnershipHTTPRejectsWithoutSideEffects` 使用真实 HTTP Handler 和 PostgreSQL，存储是专用计数内存对象（用于证明拒绝发生在读取前）；不要将这个测试称为真实 MinIO 验收。

独立的 `live-core` 与 `demo` 工作组继续使用真实 MinIO/API/Worker、Next standalone 和 Chromium。`required` 仍等待全部七个工作组，未放宽跳过、失败或取消的处理。

本地只检查 Go 语法/格式及已有门禁策略。完整编译、数据库、浏览器与容器验证以候选提交对应 CI 为准；初始提交前不声明已通过。

## 限制

这只验证显式跨对象引用的工作区一致性，不是登录认证、请求者身份授权或完整租户隔离。#97 提到的 `entity_mapping` 唯一键缺少 workspace 仍未修复。新旧数据没有迁移、回填或删除；历史数据中已经存在的错误不会由此自动纠正。

#95 非有限数值和 #96 歧义匹配仍是独立候选，不宣称已在此分支集成。新数据的生产任务界面没有在本增量实现。先处理生产命令的引用边界及幂等/重试契约，再开放相应界面。
